#
~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [-f /opt/local/etc/profile.d/bash_completion.sh]; then ./

#if [ -e /home/monte/.nix-profile/etc/profile.d/nix.sh ]; then . /home/monte/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
